# Tests layout

По умолчанию `pytest` собирает только `src/tests/hw` и `src/tests/units` согласно `pytest.ini`.

`src/tests/old` является архивом и в обычный прогон не входит.

Подробная сводка по текущему набору тестов лежит в корневом `TESTS_PLAN.md`.
