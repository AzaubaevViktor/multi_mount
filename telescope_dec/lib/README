Private PlatformIO libraries for the DEC controller firmware.

Current state:

- no private firmware libraries are extracted yet;
- all runtime logic lives in `src/main.cpp`;
- add a subdirectory here only when some firmware code becomes reusable enough to warrant its own module.
