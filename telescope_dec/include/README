Project-specific headers for the DEC controller firmware.

Current state:

- the firmware is still implemented as a single translation unit in `src/main.cpp`;
- this directory is intentionally empty for now;
- if shared declarations/constants are split out of `main.cpp`, they should live here.
