PlatformIO-side firmware tests for the DEC controller.

Current state:

- no on-device PlatformIO tests are checked in;
- verification is currently done from Python hardware tests in `src/tests/hw`;
- add MCU-specific tests here if a part of the firmware needs target-side coverage.
