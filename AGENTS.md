# Development rules
- No magic strings or numbers in logic code, use enums only; but you can use strings and values in logs and tests
- Each class must verify consistency, or be designed so consistency is ensured by default
